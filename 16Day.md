# ***12/08 Day 16***
# ***TIL (Today I Learned)***
---
### **오늘의 배운 내용**
1. `OOP` : 객체 지향 프로그래밍을 의미함
<br>*ex)```클래스: Car(브랜드,차종,연비,배기량...```*<br>*```인스턴스(객체): new Car(현대,제네시스,4000cc))```*
2. `AJAX` : 웹페이지를 새로고침하지 않고 서버와 데이터를 주고받아<br> 페이지 일부를 동적으로 업데이트하는 기술을 의미
   <br>*ex)```-sync: 동기, async: 비동기, promise: 비동기 작업의 완료·실패를 다루는 객체,```*<br>*``` fetch: 데이터를 가져오거나 보내는 함수, finally: 성공/실패 여부와 상관없이 마지막에 항상 실행```*<br>*```xmlhttprequest(xhr): 서버와 데이터를 주고받기 위해 만든 구식 자바스크립트...```*
3. `async-await` : 비동기 처리를 더 간단하고 직관적으로 작성하게 해주는 문법
   <br>*ex)```async함수: async function func ()... / await: const data = await promise;,```*<br>*```에러 처리: try { const data = await fetch(url); catch (error) {console.error(error);}}```*
---
아직도 어렵...🥲😂🤣